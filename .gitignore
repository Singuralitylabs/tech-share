# Marp の生成物。decks/ のデッキと themes/ から再生成できるのでソースではない。
# 生成コマンドは CLAUDE.md / README.md 参照。
build/

# -o を付け忘れると生成物がデッキの隣（decks/）に落ちるので、その保険。
# 追跡対象のソースを巻き込まないよう、decks/ 配下に限定して無視する。
decks/*.html
decks/*.pdf
decks/*.pptx
decks/*.png

# macOS
.DS_Store
