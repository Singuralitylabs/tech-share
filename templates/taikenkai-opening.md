---
marp: true
theme: singularity
size: 16:9
paginate: false
lang: ja
---

<!--
【転記ガイド（このコメントブロック自体は転記対象外。転記対象は下の `<!-- _paginate: skip -->` 行から EOF まで）】
- 企画 Issue に `体験会` ラベルが付いたデッキでは、下の `_paginate: skip` 行から始まる内容を、
  デッキの front matter の直後・表紙（_class: lead）スライドの前に、順序どおり・文言を改変せずに転記する
  （省略・並べ替え禁止）。front matter とこの案内コメント自体は転記しない。
- 各スライド先頭のディレクティブ（_paginate: skip と _footer: ''）も一緒に、スライドごとに転記する
  （_paginate / _footer はそのスライド1枚にしか効かないスポットディレクティブなので、複数枚に差し替えたときは
  各スライドの先頭に書く。`skip` は「ページ番号を隠す」だけでなく「本編のカウントからも除外する」ため、
  `false` にすると本編スライドの番号が定型スライドの枚数分ずれる）。
- 転記の最後（表紙スライドの直前）に `---` を入れ、表紙スライドと合体しないようにする。
- この front matter はこのファイル単体をプレビューするためのもの。デッキへは転記しない。
- 末尾用の定型スライドは `templates/taikenkai-closing.md`（別ファイル）。

⚠️ 現在の中身は仮置き（プレースホルダ）。実内容への差し替えは Issue #7 で対応する。
   `体験会` ラベル付きデッキを作る際、この仮置きのまま転記してはいけない
   （プレースホルダのままなら差し替え待ちである旨を発表者に確認する）。
-->

<!-- _paginate: skip -->
<!-- _footer: '' -->

## （仮置き）体験会 冒頭スライド

<!-- TODO: Issue #7 で元画像を反映後、このスライド群を実際の内容（複数枚）に差し替える -->
